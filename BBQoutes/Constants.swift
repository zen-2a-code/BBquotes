//
// Constants used across the app.
//

// Why: avoid magic strings and typos. Keep only broadly reused values here.

enum Constants { // Namespace for static constants (avoids global vars)
    static let bbName = "Breaking Bad"   // Tab, assets, API
    static let bcsName = "Better Call Saul" // Tab, assets, API
    static let ecName = "El Camino"        // Tab, assets, API
}
